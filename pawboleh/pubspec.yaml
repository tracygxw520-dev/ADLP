name: pawboleh
description: PawBoleh — marketing automation UI for local fashion MSMEs.
publish_to: 'none'
version: 1.0.0+1

environment:
  # Matches the Dart SDK bundled with the current Flutter stable release.
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
